<%= form_for(@task) do |f| %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <% unless @task.id.nil? %>
    <%= f.label :completed %>
    <%= f.check_box :completed %>
  <% end %>
  <%= f.submit %>
<% end %>
